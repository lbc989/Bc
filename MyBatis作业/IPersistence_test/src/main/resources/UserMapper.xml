<mapper namespace="com.lbc.dao.IUserDao">

    <select id="findAll" resultType="com.lbc.pojo.User">
        select * from user
    </select>

    <select id="findByCondition" resultType="com.lbc.pojo.User" parameterType="com.lbc.pojo.User">
        select * from user where id = #{id} and username = #{username}
    </select>

    <insert id="addUser" parameterType="com.lbc.pojo.User">
        insert into user (id,username) values(#{id},#{username})
    </insert>

    <update id="updateUser" parameterType="com.lbc.pojo.User">
        update user set username = #{username} where id = #{id}
    </update>

    <delete id="deleteUser" parameterType="int">
        delete from user where id = #{id}
    </delete>

</mapper>